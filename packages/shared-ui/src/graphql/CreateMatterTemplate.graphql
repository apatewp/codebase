mutation CreateMatterTemplate($name: String!, $javascriptModule: String!) {
  createMatterTemplate(
    input: {
      matterTemplate: { name: $name, javascriptModule: $javascriptModule }
    }
  ) {
    matterTemplate {
      id
      name
      javascriptModule
    }
  }
}
